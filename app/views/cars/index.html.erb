<div class="container">
  <div class="d-flex justify-content-between">
<% cl_image_tag("https://res.cloudinary.com/dnvzgtwcu/image/upload/v1693421326/Ferrari-SP48-Unica_3_etwocr.jpg",
      width: 300, height: 300, crop: :fill) %>
      </div>
    </div>

<div class="container">
 <div class="d-flex justify-content-between">
  <div class="row">
    <% @cars.each do |car| %>
      <div class="col-3">
        <div class="card-category">
         <%# style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path car.photo.key, height: 300, width: 300, crop: :fill %>
          <h1 class="text-light"> <%= link_to car.brand, car_path(car) %></h1>
            <p class="text-light"><%= car.title %></p>
        </div>
      </div>
    <% end %>
  </div>
</div>
